---
phase: 02-core-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - site/templates/servizi.php
  - site/templates/servizio.php
  - site/blueprints/pages/servizi.yml
  - site/blueprints/pages/servizio.yml
  - assets/css/pages/servizi.css
  - assets/css/pages/servizio.css
  - assets/js/pages/servizio.js
  - site/snippets/scripts.php
autonomous: true
requirements:
  - SERV-01
  - SERV-02
  - SERV-07
  - SERV-08
  - PERF-04

must_haves:
  truths:
    - "The /servizi page renders a grid of service cards from child pages"
    - "Each service card links to its detail page"
    - "The servizio detail page renders hero, panoramica, cosa-include, il-processo timeline, and FAQ accordion sections"
    - "The sidebar scroll-spy highlights the current section as user scrolls"
    - "FAQ accordion opens one item at a time with GSAP height animation"
    - "Process timeline has a scroll-driven progress bar"
    - "Hero images use Unsplash URLs with loading=eager"
  artifacts:
    - path: "site/templates/servizi.php"
      provides: "Service listing page template"
      contains: "children()->listed()"
    - path: "site/templates/servizio.php"
      provides: "Service detail page template"
      contains: "data-accordion"
    - path: "site/blueprints/pages/servizi.yml"
      provides: "Listing page blueprint"
      contains: "servizi"
    - path: "site/blueprints/pages/servizio.yml"
      provides: "Service detail blueprint with tabs"
      contains: "panoramica_body"
    - path: "assets/css/pages/servizi.css"
      provides: "Service listing page styles"
      contains: "service-card"
    - path: "assets/css/pages/servizio.css"
      provides: "Service detail page styles"
      contains: "service-accordion"
    - path: "assets/js/pages/servizio.js"
      provides: "FAQ accordion, scroll-spy, process timeline JS"
      contains: "data-accordion"
  key_links:
    - from: "site/templates/servizi.php"
      to: "content/servizi/*/servizio.txt"
      via: "$page->children()->listed()"
      pattern: "children.*listed"
    - from: "site/templates/servizio.php"
      to: "site/blueprints/pages/servizio.yml"
      via: "Kirby template-blueprint matching by name"
      pattern: "servizio"
    - from: "site/snippets/scripts.php"
      to: "assets/js/pages/servizio.js"
      via: "$pageJs['servizio'] conditional include"
      pattern: "servizio.*servizio\\.js"
    - from: "site/snippets/head.php"
      to: "assets/css/pages/servizio.css"
      via: "$pageCss['servizio'] conditional include (already registered)"
      pattern: "servizio.*servizio\\.css"
---

<objective>
Create the complete service page infrastructure: listing template (/servizi) and detail template (/servizi/diritto-di-famiglia etc.) with all supporting blueprints, CSS, and JS. The listing page shows a card grid of child services. The detail page has a hero, sidebar scroll-spy navigator, panoramica section, cosa-include grid, process timeline with scroll-driven progress, and FAQ accordion.

Purpose: Build the template layer that will render the four service content pages (seeded in Plan 03). These templates are the primary SEO surface of the site.
Output: 2 templates, 2 blueprints, 2 CSS files, 1 JS file, scripts.php update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-pages/02-RESEARCH.md

@site/snippets/head.php
@site/snippets/scripts.php
@assets/css/variables.css

<interfaces>
<!-- Reference patterns to adapt (read these during execution): -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\site\templates\servizi.php — listing template -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\site\templates\servizio.php — detail template -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\site\blueprints\pages\servizio.yml — detail blueprint -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\pages\servizio.css — detail CSS -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\assets\js\pages\servizio.js — detail JS -->

<!-- head.php already has servizi and servizio in $pageCss map (lines 39-44): -->
'servizi'     => 'assets/css/pages/servizi.css',
'servizio'    => 'assets/css/pages/servizio.css',

<!-- scripts.php needs servizio added to $pageJs (currently only has contatti and home) -->

<!-- Design tokens from variables.css: -->
--primary: #1a2744; --secondary: #b8960c;
--font-heading: 'Jost'; --font-body: 'Manrope';
--radius: 16px; --radius-sm: 4px;
--sp-1 through --sp-10 (0.25rem to 8rem)
--shadow-sm/md/lg; --ease; --dur; --dur-slow;
--grey-50 through --grey-800;

<!-- Existing homepage CSS patterns to reuse: -->
<!-- .btn, .btn--primary, .btn--accent classes from homepage.css -->
<!-- .subtitle class from homepage.css -->
<!-- .section, .section--light, .section--dark from main.css -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create servizi listing page (template + blueprint + CSS + content stub)</name>
  <files>
    site/templates/servizi.php
    site/blueprints/pages/servizi.yml
    assets/css/pages/servizi.css
    content/servizi/servizi.txt
  </files>
  <read_first>
    - C:\Users\boxwe\Documents\GitHub\studioatheste\site\templates\servizi.php
    - C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\pages\servizi.css
    - site/snippets/head.php
    - site/snippets/header.php
    - site/snippets/footer.php
    - site/templates/home.php
    - assets/css/variables.css
  </read_first>
  <action>
Create 4 files for the /servizi listing page. SIMPLIFY from studioatheste -- no pilastri, no partners, no perche-noi sections. Just: page hero + service cards grid + CTA.

**1. site/blueprints/pages/servizi.yml:**
```yaml
title: Servizi

fields:
  hero_title:
    type: text
    label: Titolo Hero
  hero_subtitle:
    type: textarea
    label: Sottotitolo Hero
    size: small
  servizi_eyebrow:
    type: text
    label: Eyebrow Sezione Servizi
  servizi_title:
    type: text
    label: Titolo Sezione Servizi
  servizi_description:
    type: textarea
    label: Descrizione Sezione Servizi
    size: small
  cta_title:
    type: text
    label: Titolo CTA
  cta_description:
    type: textarea
    label: Descrizione CTA
    size: small
  cta_button_text:
    type: text
    label: Testo Pulsante CTA
  cta_button_url:
    type: text
    label: URL Pulsante CTA
  meta_title:
    type: text
    label: Meta Title
  meta_description:
    type: textarea
    label: Meta Description
```

**2. site/templates/servizi.php:**
Simplified from studioatheste. Sections:
- `snippet('head')`, `snippet('header')`
- Page hero: `section.page-hero` with Unsplash background image (Italian courthouse/legal documents, NOT gavel), breadcrumb (Home / Servizi), h1 from `$page->hero_title()->or($page->title())`, subtitle. Hero image: `https://images.unsplash.com/photo-1589829545856-d10d557cf95f?w=1920&q=80` with `loading="eager"`.
- Services grid: `section.section` with `section-header` (eyebrow, title, description from content fields with `isNotEmpty()` checks) and `div.services-grid`. Use `$page->children()->listed()` to loop through child service pages. Each renders as `a.service-card` linking to `$servizio->url()` with: `.service-card__icon` (Lucide icon from `$servizio->icon()`), `h3` from `$servizio->card_title()->or($servizio->title())`, `p` from `$servizio->card_description()`, arrow icon.
- CTA section: `section.cta-section` with Unsplash background (professional consultation), title, description, button from content fields.
- `snippet('footer')`, `snippet('scripts')`

Use same page-hero pattern as studioatheste but with inline Unsplash img instead of file picker. Breadcrumb markup: `ol.breadcrumb` with Home link + current page.

**3. assets/css/pages/servizi.css:**
Use `@layer layout { }` wrapper consistent with homepage.css. Use project design tokens from variables.css (--primary, --secondary, --sp-*, --radius, --font-heading, --font-body, --grey-*, --shadow-*).

CSS must include:
- `.page-hero` styles: full-width, min-height ~50vh (not 100vh like homepage), relative positioning, bg image cover, overlay rgba(0,0,0,.5), content centered.
- `.page-hero__title`: clamp(32px, 4vw, 52px), color white, font-heading.
- `.page-hero__subtitle`: 17px, color rgba(255,255,255,.65).
- `.breadcrumb`: flex row, font-size 13px, color rgba(255,255,255,.5), links white on hover.
- `.section-header`: text-align center, margin-bottom 50px, max-width 680px, margin-inline auto.
- `.section-header__eyebrow`: same pattern as .subtitle (12px, uppercase, letter-spacing .2em, color secondary).
- `.section-header__title`: font-heading, font-size clamp(28px, 3.5vw, 42px).
- `.section-header__description`: color grey-600, max-width 560px, margin-inline auto.
- `.services-grid`: display grid, 2 columns on desktop (min-width 768px), 1 column mobile, gap 24px.
- `.service-card`: padding 32px, text-decoration none, color heading-color, background white, border 1px solid grey-200, border-radius var(--radius), transition all var(--dur) var(--ease). Hover: translateY(-4px), shadow-md, border-color secondary.
- `.service-card__icon`: 60px circle, background rgba(secondary-rgb, .1), flex center, margin-bottom 20px. Icon 28px, color secondary.
- `.service-card__title`: font-heading, 22px, margin-bottom 8px.
- `.service-card__description`: font-size 15px, color grey-600, line-height 1.7.
- `.service-card__arrow`: display block, margin-top 16px, color secondary, 20px icon.
- CTA section: reuse `.cta-section` styles from homepage.css (already defined in homepage.css in @layer layout). If collision risk, scope with `.servizi-cta` or verify homepage.css CTA styles are in a shared layer.

NOTE: The `.cta-section` styles already exist in homepage.css. Since head.php loads both main.css and page-specific CSS, and both use `@layer layout`, the CTA styles from homepage.css will cascade. Do NOT duplicate `.cta-section` styles in servizi.css -- only add styles unique to the servizi page.

**4. content/servizi/servizi.txt:**
```
Title: Servizi Legali

----

Hero-title: I Nostri Servizi Legali

----

Hero-subtitle: Assistenza professionale in tutte le principali aree del diritto civile

----

Servizi-eyebrow: Aree di Pratica

----

Servizi-title: Competenze al Servizio dei Vostri Diritti

----

Servizi-description: Lo Studio Legale Zanin offre assistenza qualificata in quattro aree principali del diritto civile, con un approccio personalizzato per ogni cliente.

----

Cta-title: Hai Bisogno di Assistenza Legale?

----

Cta-description: Prenota una prima consulenza per valutare insieme il tuo caso.

----

Cta-button-text: Contattaci Ora

----

Cta-button-url: contatti

----

Meta-title:

----

Meta-description:

----

Uuid: servizi
```

Create the content/servizi/ directory first if it does not exist.
  </action>
  <verify>
    <automated>test -f site/templates/servizi.php && test -f site/blueprints/pages/servizi.yml && test -f assets/css/pages/servizi.css && test -f content/servizi/servizi.txt && grep -q "children.*listed" site/templates/servizi.php && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `site/templates/servizi.php` exists and contains `$page->children()->listed()`
    - `site/templates/servizi.php` contains `service-card` class
    - `site/templates/servizi.php` contains `breadcrumb` markup
    - `site/templates/servizi.php` contains `loading="eager"` on hero image
    - `site/blueprints/pages/servizi.yml` exists and contains `hero_title`, `servizi_eyebrow`, `cta_title`, `meta_title`
    - `assets/css/pages/servizi.css` exists and contains `@layer layout`
    - `assets/css/pages/servizi.css` contains `.page-hero`, `.services-grid`, `.service-card`
    - `content/servizi/servizi.txt` exists and contains `Title: Servizi Legali`
    - No `.cta-section` duplicate styles in servizi.css (reuses from homepage.css)
  </acceptance_criteria>
  <done>The /servizi listing page has a template rendering child service cards dynamically, a blueprint for Panel editing, CSS styles using project design tokens, and a content file with Italian copy.</done>
</task>

<task type="auto">
  <name>Task 2: Create servizio detail page (template + blueprint + CSS)</name>
  <files>
    site/templates/servizio.php
    site/blueprints/pages/servizio.yml
    assets/css/pages/servizio.css
  </files>
  <read_first>
    - C:\Users\boxwe\Documents\GitHub\studioatheste\site\templates\servizio.php
    - C:\Users\boxwe\Documents\GitHub\studioatheste\site\blueprints\pages\servizio.yml
    - C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\pages\servizio.css
    - assets/css/variables.css
    - site/templates/home.php
  </read_first>
  <action>
Create 3 files for the service detail template. Adapt from studioatheste but SIMPLIFY:
- No hero_video/hero_image file pickers (use inline Unsplash URL per service, passed from content field)
- No hero_badges
- No callout_counters / service-highlights section
- No related_services section (only 4 services, cross-linking added later if needed)
- Referent is always "Avv. Sebastiano Zanin" -- hardcode in sidebar, no content fields needed

**1. site/blueprints/pages/servizio.yml:**
Tabs structure (adapted from reference, simplified):

```yaml
title: Servizio

tabs:
  hero:
    label: Hero
    icon: image
    fields:
      hero_title:
        label: Titolo Hero
        type: text
        required: true
      hero_subtitle:
        label: Sottotitolo Hero
        type: textarea
        size: small
      hero_image_url:
        label: URL Immagine Hero
        type: url
        help: "URL Unsplash dell'immagine hero (es. https://images.unsplash.com/...?w=1920&q=80)"

  contenuto:
    label: Contenuto
    icon: text
    fields:
      panoramica_body:
        label: Panoramica - Testo
        type: textarea
        help: Testo della sezione panoramica (supporta Markdown)
      includes:
        label: Cosa Include
        type: structure
        help: "Le voci del servizio (4-6 consigliate)"
        fields:
          icon:
            label: Icona
            type: text
            width: 1/4
            placeholder: file-text
            help: "Nome icona da lucide.dev"
          title:
            label: Titolo
            type: text
            width: 3/4
            required: true
          description:
            label: Descrizione
            type: textarea
            size: small
      process_steps:
        label: Il Processo
        type: structure
        help: "Le fasi del processo legale (4-5 consigliate)"
        fields:
          step:
            label: Numero Step
            type: text
            width: 1/4
            placeholder: "01"
          title:
            label: Titolo
            type: text
            width: 3/4
            required: true
          description:
            label: Descrizione
            type: textarea
            size: small
      faq:
        label: FAQ
        type: structure
        help: "Domande frequenti (4-6 consigliate)"
        fields:
          question:
            label: Domanda
            type: text
            required: true
          answer:
            label: Risposta
            type: textarea
            size: small

  card:
    label: Card
    icon: grid
    fields:
      icon:
        label: Icona Card
        type: text
        help: "Nome icona Lucide per la card nella pagina servizi"
        placeholder: users
      card_title:
        label: Titolo Card
        type: text
        help: "Titolo per la card nella pagina listing"
      card_description:
        label: Descrizione Card
        type: textarea
        size: small

  cta:
    label: CTA
    icon: bolt
    fields:
      cta_title:
        label: Titolo CTA
        type: text
        placeholder: Hai bisogno di assistenza?
      cta_description:
        label: Descrizione CTA
        type: textarea
        size: small
      cta_button_text:
        label: Testo Pulsante
        type: text
        placeholder: Contattaci
      cta_button_url:
        label: URL Pulsante
        type: text
        help: "Slug della pagina (es. contatti)"

  seo:
    label: SEO
    icon: search
    fields:
      meta_title:
        label: Meta Title
        type: text
      meta_description:
        label: Meta Description
        type: textarea
        size: small
```

**2. site/templates/servizio.php:**
Adapted from studioatheste servizio.php. Structure:

```
snippet('head') + snippet('header')
<main id="main-content">

  <!-- PAGE HERO -->
  section.page-hero:
    - Hero bg: img from $page->hero_image_url() with fallback to generic Unsplash URL. loading="eager".
    - Overlay div.page-hero__overlay
    - Container with breadcrumb: Home / Servizi / Current page title
    - h1.page-hero__title from hero_title()->or(title())
    - p.page-hero__subtitle if isNotEmpty()

  <!-- SIDEBAR + MAIN CONTENT GRID -->
  section.section > container > div.service-navigator:

    Main content (div.service-navigator__main):

    A. #panoramica: h2 "Panoramica", body from panoramica_body()->kirbytext()

    B. #cosa-include (if includes()->isNotEmpty()):
       h2 "Cosa include il servizio"
       div.service-includes grid with foreach includes()->toStructure():
         - .service-includes__item with icon, title, description

    C. #il-processo (if process_steps()->isNotEmpty()):
       h2 "Il nostro processo"
       div.process-timeline with track + progress bar + foreach steps:
         - .process-timeline__step with marker (number), title, description

    D. #faq (if faq()->isNotEmpty()):
       h2 "Domande frequenti"
       div.service-accordion[data-accordion="faq"] with foreach faq:
         - .service-accordion__item[data-accordion-item]
         - button.service-accordion__btn with aria-expanded="false", question text, chevron-down icon
         - div.service-accordion__panel[aria-hidden="true"] > .service-accordion__panel-inner > p answer

    Sidebar (aside.service-sidebar):

    - nav.service-sidebar__nav with ul.service-sidebar__list:
      - li > a.service-sidebar__link[href="#panoramica"] "Panoramica" (is-active default)
      - li > a[href="#cosa-include"] "Cosa include"
      - li > a[href="#il-processo"] "Il Processo"
      - li > a[href="#faq"] "FAQ"

    - div.service-sidebar__contact:
      - h3 "Il vostro referente"
      - p.service-sidebar__contact-name "Avv. Sebastiano Zanin" (hardcoded)
      - p.service-sidebar__contact-role "Avvocato Civilista" (hardcoded)
      - Contact items: phone (from $site->phone()), email (from $site->email())
      - CTA button: "Contattaci" linking to /contatti

  <!-- CTA FINALE -->
  section.cta-section (if cta_title()->isNotEmpty()):
    - bg img from Unsplash, overlay
    - title, description, button from content fields

</main>
snippet('footer') + snippet('scripts')
```

Use exact same class names as studioatheste so the CSS and JS work without changes. Key difference: hero uses inline Unsplash URL from `hero_image_url` field instead of file picker.

**3. assets/css/pages/servizio.css:**
Adapt studioatheste's servizio.css to use THIS project's design tokens. Key mappings:

| studioatheste token | avvocatozanin token |
|---|---|
| `var(--color-white)` | `var(--white)` |
| `var(--color-gray-200)` | `var(--grey-200)` |
| `var(--color-gray-400)` | `var(--grey-400)` |
| `var(--color-gray-600)` | `var(--grey-600)` |
| `var(--color-gray-700)` | `var(--grey-600)` (closest) |
| `var(--color-gray-900)` | `var(--grey-800)` |
| `var(--color-gray-50)` | `var(--grey-50)` |
| `var(--color-primary)` | `var(--secondary)` (gold is the accent in this project) |
| `var(--color-primary-light)` | `var(--secondary-light)` |
| `var(--color-text)` | `var(--heading-color)` |
| `var(--color-text-light)` | `var(--body-color)` |
| `var(--font-display)` | `var(--font-heading)` |
| `var(--font-body)` | `var(--font-body)` |
| `var(--fs-h2)` | `clamp(28px, 3.5vw, 42px)` |
| `var(--fs-h3)` | `var(--h3)` / `24px` |
| `var(--fs-h4)` | `var(--h4)` / `20px` |
| `var(--fs-body)` | `var(--body-size)` / `16px` |
| `var(--fs-small)` | `14px` |
| `var(--fw-semibold)` | `600` |
| `var(--fw-bold)` | `700` |
| `var(--fw-medium)` | `500` |
| `var(--lh-tight)` | `1.2` |
| `var(--lh-relaxed)` | `1.7` |
| `var(--radius-lg)` | `var(--radius)` (16px) |
| `var(--space-N)` | `var(--sp-N)` |
| `var(--duration-fast)` | `var(--dur)` |
| `var(--duration-normal)` | `var(--dur)` |
| `var(--ease-out)` | `var(--ease)` |
| `var(--shadow-md)` | `var(--shadow-md)` |
| `var(--header-height)` | `80px` (no CSS var defined, use literal or add to variables.css) |

Include in servizio.css (wrapped in `@layer layout { }`):
- `.page-hero` styles (reuse from servizi.css -- OR define shared page-hero styles in servizi.css and import both). Better: define `.page-hero` only in servizi.css since both pages need it and servizi.css loads for servizi template while servizio.css loads for servizio template. Problem: they load separately. Solution: put `.page-hero` styles in BOTH files (acceptable duplication for 2 pages) OR create a shared snippet. Simplest: put page-hero in servizio.css too since the selector is the same and CSS @layer deduplicates.
- `.service-navigator` grid: 1fr on mobile, `1fr 280px` at 768px, `1fr 340px` at 1280px
- `.service-sidebar`: sticky on desktop (top: 104px = 80px header + 24px gap), order -1 on mobile
- `.service-sidebar__nav`, `.service-sidebar__list`, `.service-sidebar__link` with `is-active` state (border-left 3px solid secondary)
- `.service-sidebar__contact` card with grey-50 background
- `.service-section` with scroll-margin-top, margin-bottom
- `.service-section__title`, `.service-section__body` paragraph styles
- `.service-includes` grid (2 cols at 768px)
- `.service-accordion__item`, `__btn`, `__panel` (height: 0, overflow: hidden), `__panel-inner`, `__chevron` rotation
- `.process-timeline` with vertical track, progress bar, step markers, active state
- `@media (prefers-reduced-motion: reduce)` overrides

DO NOT include hero-badges, service-highlights/callout, or related-services styles (those sections are removed from the simplified template).

Add `.page-hero` styles in servizio.css (same as servizi.css page-hero styles -- this duplication is acceptable since they load on different pages).
  </action>
  <verify>
    <automated>test -f site/templates/servizio.php && test -f site/blueprints/pages/servizio.yml && test -f assets/css/pages/servizio.css && grep -q "data-accordion" site/templates/servizio.php && grep -q "panoramica_body" site/blueprints/pages/servizio.yml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `site/templates/servizio.php` contains sections: `#panoramica`, `#cosa-include`, `#il-processo`, `#faq`
    - `site/templates/servizio.php` contains `data-accordion="faq"` and `data-accordion-item`
    - `site/templates/servizio.php` contains `aria-expanded="false"` on accordion buttons
    - `site/templates/servizio.php` contains breadcrumb with Home / Servizi / current
    - `site/templates/servizio.php` contains `loading="eager"` on hero image
    - `site/templates/servizio.php` contains hardcoded "Avv. Sebastiano Zanin" in sidebar
    - `site/blueprints/pages/servizio.yml` has tabs: hero, contenuto, card, cta, seo
    - `site/blueprints/pages/servizio.yml` has structure fields: includes, process_steps, faq
    - `assets/css/pages/servizio.css` wrapped in `@layer layout`
    - `assets/css/pages/servizio.css` contains `.service-accordion__panel` with `height: 0` and `overflow: hidden`
    - `assets/css/pages/servizio.css` contains `.process-timeline` styles
    - `assets/css/pages/servizio.css` contains `prefers-reduced-motion` media query
    - `assets/css/pages/servizio.css` uses project tokens (--primary, --secondary, --sp-*, --font-heading)
  </acceptance_criteria>
  <done>Service detail template renders hero, sidebar with scroll-spy nav and contact card, panoramica, cosa-include grid, process timeline with progress bar, and FAQ accordion. Blueprint supports all fields. CSS uses project design tokens. All structure fields have isNotEmpty() guards.</done>
</task>

<task type="auto">
  <name>Task 3: Create servizio.js and register in scripts.php</name>
  <files>
    assets/js/pages/servizio.js
    site/snippets/scripts.php
  </files>
  <read_first>
    - C:\Users\boxwe\Documents\GitHub\studioatheste\assets\js\pages\servizio.js
    - site/snippets/scripts.php
    - site/templates/servizio.php
  </read_first>
  <action>
**1. assets/js/pages/servizio.js:**
Adapt from studioatheste servizio.js. Self-contained IIFE. Include:

A. **Scroll-spy** (section 1 from reference):
- Query `.service-sidebar__link` and `.service-section` elements
- For each section, create ScrollTrigger with start 'top 40%', end 'bottom 40%', onToggle adds/removes `is-active` class on matching sidebar link
- Smooth scroll on sidebar link click with header offset (80px + 24px)

B. **Process timeline** (section 2a from reference):
- Each `.process-timeline__step` fades in (opacity 0, y 24 -> opacity 1, y 0) on scroll
- Each step gets `is-active` class when in viewport center
- Progress bar `.process-timeline__progress` height animates from 0 to 100% with scrub
- Null-check timeline element before running

C. **FAQ Accordion** (section 2c from reference -- copy nearly verbatim):
- Query `[data-accordion]` groups
- For each group, query `[data-accordion-item]` items
- Click handler: close all other open items (exclusive open), toggle current
- Open: add `is-open` class, set `aria-expanded="true"`, `aria-hidden="false"`, gsap.to panel height = panelInner.offsetHeight
- Close: remove `is-open`, aria attrs, gsap.to panel height = 0
- Duration 0.4s, ease power2.inOut
- After accordion toggle, call `ScrollTrigger.refresh()` to recalculate positions

D. **Fade-in sections** (section 5 from reference):
- All `.service-section` elements get opacity 0 -> 1, y 30 -> 0 fade on scroll
- Exclude `#cosa-include` if it has the pinned reveal (but we're NOT doing pinned reveal in this simplified version -- so include all sections)

E. **Reduced motion guard** at top of IIFE:
```javascript
if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
  // Still init accordion click handlers (functional, not decorative)
  // but skip all GSAP animations
}
```
Wait -- the accordion is functional, not just decorative. The reduced-motion guard should skip animations but still allow accordion open/close. Approach: separate the accordion click logic (always runs) from the GSAP animation parts (skipped under reduced motion). Under reduced motion, use `panel.style.height = 'auto'` for open and `panel.style.height = '0'` for close instead of GSAP.

Structure:
```javascript
(function() {
  'use strict';
  var reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  // A. SCROLL-SPY (skip if reducedMotion -- links still work via native scroll)
  if (!reducedMotion) { /* scroll-spy code */ }

  // B. PROCESS TIMELINE (skip if reducedMotion)
  if (!reducedMotion) { /* timeline code */ }

  // C. FAQ ACCORDION (always runs, but uses CSS transitions if reducedMotion)
  // accordion code with conditional: if reducedMotion, set height directly; else use gsap.to

  // D. FADE-IN (skip if reducedMotion)
  if (!reducedMotion) { /* fade-in code */ }

  // Sidebar link click: always works (smooth scroll native)
  // Move sidebar click handlers outside reduced-motion guard
})();
```

**2. site/snippets/scripts.php:**
Add `'servizio'` to the `$pageJs` array. The current array is:
```php
$pageJs = [
    'contatti' => ['assets/js/pages/contatti.js'],
    'home'     => ['assets/js/pages/home.js'],
];
```
Add after the 'home' entry:
```php
$pageJs = [
    'contatti' => ['assets/js/pages/contatti.js'],
    'home'     => ['assets/js/pages/home.js'],
    'servizio' => ['assets/js/pages/servizio.js'],
];
```
  </action>
  <verify>
    <automated>test -f assets/js/pages/servizio.js && grep -q "servizio" site/snippets/scripts.php && grep -q "data-accordion" assets/js/pages/servizio.js && grep -q "ScrollTrigger" assets/js/pages/servizio.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `assets/js/pages/servizio.js` exists and is > 100 lines
    - Contains scroll-spy: `grep "service-sidebar__link" assets/js/pages/servizio.js` matches
    - Contains accordion: `grep "data-accordion" assets/js/pages/servizio.js` matches
    - Contains timeline: `grep "process-timeline" assets/js/pages/servizio.js` matches
    - Contains reduced motion handling: `grep "prefers-reduced-motion" assets/js/pages/servizio.js` matches
    - Contains `aria-expanded` toggle for accordion accessibility
    - Contains `ScrollTrigger.refresh` after accordion toggle
    - `site/snippets/scripts.php` contains `'servizio' => ['assets/js/pages/servizio.js']`
    - scripts.php still contains `'home'` and `'contatti'` entries (not deleted)
  </acceptance_criteria>
  <done>servizio.js provides scroll-spy, process timeline animation, FAQ accordion with ARIA, and section fade-ins. Accordion works under reduced motion (no GSAP, direct height toggle). scripts.php loads servizio.js on servizio template pages.</done>
</task>

</tasks>

<verification>
1. `test -f site/templates/servizi.php` -- listing template exists
2. `test -f site/templates/servizio.php` -- detail template exists
3. `test -f site/blueprints/pages/servizi.yml` -- listing blueprint exists
4. `test -f site/blueprints/pages/servizio.yml` -- detail blueprint exists
5. `test -f assets/css/pages/servizi.css` -- listing CSS exists
6. `test -f assets/css/pages/servizio.css` -- detail CSS exists
7. `test -f assets/js/pages/servizio.js` -- detail JS exists
8. `grep "servizio" site/snippets/scripts.php` -- JS registered
9. `test -f content/servizi/servizi.txt` -- listing content exists
10. `grep "data-accordion" site/templates/servizio.php` -- accordion markup present
11. `grep "children.*listed" site/templates/servizi.php` -- dynamic card rendering
</verification>

<success_criteria>
- servizi.php renders a card grid from child pages with icons, titles, descriptions, and arrows
- servizio.php renders hero + sidebar scroll-spy + panoramica + cosa-include + process timeline + FAQ accordion + CTA
- All blueprint fields support Panel editing
- CSS uses project design tokens consistently
- servizio.js provides scroll-spy, accordion, timeline, and fade-in animations
- Accordion is functional even under prefers-reduced-motion
- scripts.php conditionally loads servizio.js
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-02-SUMMARY.md`
</output>
