---
phase: 02-core-pages
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified: []
autonomous: false
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05
  - HOME-06
  - SERV-01
  - SERV-02
  - SERV-03
  - SERV-04
  - SERV-05
  - SERV-06
  - SERV-07
  - SERV-08
  - PERF-04

must_haves:
  truths:
    - "Homepage animations play correctly on scroll"
    - "Service listing shows 4 cards linking to detail pages"
    - "Service detail pages render all sections with content"
    - "FAQ accordion opens and closes items"
    - "No console errors on any page"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of all Phase 2 deliverables. The user starts a local PHP server and verifies that the homepage animations, service listing, and all four service detail pages render correctly with content and interactions.

Purpose: Confirm that templates, CSS, JS, and content all work together before moving to Phase 3.
Output: Human approval or issue list for gap closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start PHP development server and run smoke checks</name>
  <files></files>
  <read_first>
    - site/templates/home.php
    - site/templates/servizi.php
    - site/templates/servizio.php
  </read_first>
  <action>
Start the PHP built-in server for testing:
```bash
php -S localhost:8000 -t . kirby/router.php
```
(Run in background or instruct user to start it.)

Then verify basic rendering with curl:
1. `curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/` -- expect 200 (homepage)
2. `curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/servizi` -- expect 200 (listing)
3. `curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/servizi/diritto-di-famiglia` -- expect 200
4. `curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/servizi/diritto-immobiliare` -- expect 200
5. `curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/servizi/risarcimento-danni` -- expect 200
6. `curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/servizi/recupero-crediti` -- expect 200

Check that key HTML elements exist:
7. `curl -s http://localhost:8000/ | grep -c "hero__title"` -- expect >= 1
8. `curl -s http://localhost:8000/servizi | grep -c "service-card"` -- expect >= 4
9. `curl -s http://localhost:8000/servizi/diritto-di-famiglia | grep -c "data-accordion-item"` -- expect >= 4
10. `curl -s http://localhost:8000/servizi/diritto-di-famiglia | grep -c "process-timeline__step"` -- expect >= 4

If any check fails, diagnose and fix the issue before proceeding to the visual checkpoint.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/ 2>/dev/null | grep -q "200" && curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/servizi 2>/dev/null | grep -q "200" && echo "PASS" || echo "FAIL — start PHP server first: php -S localhost:8000 -t . kirby/router.php"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 URLs return HTTP 200
    - Homepage contains `.hero__title` element
    - /servizi contains at least 4 `.service-card` elements
    - /servizi/diritto-di-famiglia contains at least 4 `data-accordion-item` elements
    - /servizi/diritto-di-famiglia contains at least 4 `.process-timeline__step` elements
  </acceptance_criteria>
  <done>All pages render without PHP errors, return 200, and contain expected HTML structure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Phase 2 pages</name>
  <files></files>
  <action>
Present the user with the verification checklist below. Wait for their approval or issue report. This is a visual/functional checkpoint -- no code changes unless issues are reported.
  </action>
  <what-built>
    Phase 2 complete: homepage GSAP animations, /servizi listing page with 4 service cards, and 4 service detail pages (diritto-di-famiglia, diritto-immobiliare, risarcimento-danni, recupero-crediti) each with hero, panoramica, cosa-include, process timeline, FAQ accordion, sidebar scroll-spy, and CTA section.
  </what-built>
  <how-to-verify>
    Start PHP server if not running: `php -S localhost:8000 -t . kirby/router.php`

    **Homepage (http://localhost:8000/):**
    1. Hero title animates word-by-word on page load
    2. Scroll down -- value cards stagger in
    3. About section image has subtle parallax, text fades in from right
    4. Glass-box counters animate from 0 (15+, 500+)
    5. Contact bar items fade in
    6. Practice area cards stagger in
    7. Stats section counters animate (500+, 95%, 4, 15+)
    8. Process steps stagger in
    9. CTA section has parallax background, text fades up

    **Servizi listing (http://localhost:8000/servizi):**
    10. Page hero with breadcrumb (Home / Servizi Legali)
    11. Grid of 4 service cards with icons, titles, descriptions
    12. Each card links to correct service detail page
    13. CTA section at bottom

    **Service detail (test http://localhost:8000/servizi/diritto-di-famiglia):**
    14. Page hero with breadcrumb (Home / Servizi / Diritto di Famiglia)
    15. Sidebar scroll-spy with 4 links (Panoramica, Cosa include, Il Processo, FAQ)
    16. Sidebar links highlight as you scroll through sections
    17. Panoramica section with Italian text
    18. Cosa include grid with icons
    19. Process timeline with progress bar that fills on scroll
    20. FAQ accordion -- click a question, answer slides open; click another, first closes
    21. Contact card in sidebar with Avv. Zanin info
    22. CTA section at bottom

    **Repeat quick check on remaining 3 service pages:**
    23. http://localhost:8000/servizi/diritto-immobiliare -- renders
    24. http://localhost:8000/servizi/risarcimento-danni -- renders
    25. http://localhost:8000/servizi/recupero-crediti -- renders

    **Responsiveness:** Resize browser to mobile width:
    26. Homepage sections stack correctly
    27. Service cards go to 1 column
    28. Sidebar becomes horizontal scroll on mobile
  </how-to-verify>
  <verify>
    <automated>echo "CHECKPOINT — requires human verification"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms all 28 checklist items pass
    - No console errors reported in browser DevTools
  </acceptance_criteria>
  <done>User has visually verified all Phase 2 pages and approved them for Phase 3.</done>
  <resume-signal>Type "approved" to proceed to Phase 3, or describe any issues that need fixing.</resume-signal>
</task>

</tasks>

<verification>
All verification happens in the human-verify checkpoint above.
</verification>

<success_criteria>
- User confirms all homepage animations work
- User confirms /servizi listing renders 4 cards
- User confirms all 4 service detail pages render correctly
- User confirms FAQ accordion interaction works
- User confirms sidebar scroll-spy works
- No console errors reported
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-04-SUMMARY.md`
</output>
