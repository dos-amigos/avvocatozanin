---
phase: 03-supporting-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/templates/chi-sono.php
  - site/templates/come-lavoro.php
  - site/blueprints/pages/chi-sono.yml
  - site/blueprints/pages/come-lavoro.yml
  - content/chi-sono/chi-sono.txt
  - content/come-lavoro/come-lavoro.txt
  - assets/css/pages/chi-sono.css
  - assets/css/pages/come-lavoro.css
autonomous: true
requirements:
  - TRUST-01
  - TRUST-02
  - TRUST-03
  - TRUST-04

must_haves:
  truths:
    - "Chi Sono page displays Avv. Zanin's professional biography, experience, and values"
    - "Chi Sono page shows 'Iscritto all Ordine degli Avvocati di Padova' with registration number field"
    - "Chi Sono page states 'Opero presso il Tribunale di Este e il Tribunale di Padova'"
    - "Come Lavoro page shows a 4-step legal process: consulenza, mandato, causa, risultato"
  artifacts:
    - path: "site/templates/chi-sono.php"
      provides: "Chi Sono page template with bio, credentials, jurisdiction"
      contains: "Ordine degli Avvocati di Padova"
    - path: "site/templates/come-lavoro.php"
      provides: "Come Lavoro page template with vertical timeline"
      contains: "process_steps"
    - path: "content/chi-sono/chi-sono.txt"
      provides: "Seeded Italian content for Chi Sono"
      contains: "Tribunale di Este"
    - path: "content/come-lavoro/come-lavoro.txt"
      provides: "Seeded Italian content for Come Lavoro"
      contains: "Consulenza"
  key_links:
    - from: "site/templates/chi-sono.php"
      to: "content/chi-sono/chi-sono.txt"
      via: "Kirby flat-file content"
      pattern: "page->bio_body"
    - from: "site/templates/come-lavoro.php"
      to: "content/come-lavoro/come-lavoro.txt"
      via: "Kirby structure field"
      pattern: "page->process_steps.*toStructure"
---

<objective>
Create the Chi Sono and Come Lavoro trust pages — templates, blueprints, content files, and CSS.

Purpose: These pages build trust with potential clients by showing the lawyer's credentials, jurisdiction, and working methodology. They are the second most important conversion factor after services.
Output: Two fully rendered pages at /chi-sono and /come-lavoro with professional Italian content.
</objective>

<execution_context>
@C:\Users\boxwe\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\boxwe\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-supporting-pages/03-RESEARCH.md

@site/templates/servizio.php
@site/blueprints/pages/servizio.yml
@site/snippets/head.php
@site/snippets/scripts.php
@site/snippets/footer.php
@content/site.txt
@assets/css/pages/servizio.css

<interfaces>
<!-- head.php already wires chi-sono.css and come-lavoro.css (lines 43-44) -->
<!-- scripts.php has no page-specific JS for chi-sono or come-lavoro — no JS file needed -->
<!-- footer.php already links to /chi-sono and /come-lavoro (lines 28-29) -->

From site/snippets/head.php (page CSS map, line 38-45):
```php
$pageCss = [
    'home'        => 'assets/css/pages/homepage.css',
    'servizi'     => 'assets/css/pages/servizi.css',
    'servizio'    => 'assets/css/pages/servizio.css',
    'contatti'    => 'assets/css/pages/contatti.css',
    'chi-sono'    => 'assets/css/pages/chi-sono.css',
    'come-lavoro' => 'assets/css/pages/come-lavoro.css',
];
```

From content/site.txt (site-level fields used in templates):
```
Phone: 0429.1960202
Address: Via G.B. Brunelli 12
City: 35042 Este (PD)
Pec: pec@avvocatozanin.it
```

From site/snippets/footer.php (subfooter already has Ordine reference, line 56):
```php
<p>&copy; <?= date('Y') ?> Avv. Sebastiano Zanin &mdash; P.IVA <?= $site->piva() ?> &mdash; Ordine Avvocati Padova</p>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chi Sono — blueprint, template, content, CSS</name>
  <files>
    site/blueprints/pages/chi-sono.yml,
    site/templates/chi-sono.php,
    content/chi-sono/chi-sono.txt,
    assets/css/pages/chi-sono.css
  </files>
  <read_first>
    site/templates/servizio.php,
    site/blueprints/pages/servizio.yml,
    site/snippets/head.php,
    site/snippets/footer.php,
    content/site.txt,
    assets/css/pages/servizio.css
  </read_first>
  <action>
**Blueprint (chi-sono.yml):**
Create a tabbed blueprint following the servizio.yml pattern with tabs: hero, contenuto, seo.

Hero tab fields:
- `hero_title` (text, required) — default "Chi Sono"
- `hero_subtitle` (textarea, small)
- `hero_image_url` (url) — fallback portrait image from Unsplash

Contenuto tab fields:
- `bio_body` (textarea, large) — main biography text (supports Markdown via kirbytext)
- `valori_title` (text) — heading for values section, default "I Miei Valori"
- `valori` (structure) — fields: `icon` (text, 1/4, lucide icon name), `title` (text, 1/4, required), `description` (textarea, small, 2/4)
- `ordine_numero` (text) — Numero iscrizione Ordine Avvocati di Padova
- `giurisdizione` (textarea, small) — jurisdiction statement

SEO tab: `meta_title` (text), `meta_description` (textarea, small) — left empty per Phase 2 decision.

**Template (chi-sono.php):**
Follow the servizio.php pattern: `snippet('head')`, skip-link, `snippet('header')`, `<main id="main-content">`.

Sections:
1. **Page Hero** — reuse the `page-hero` pattern from servizio (dark overlay, breadcrumb Home/Chi Sono, h1, subtitle). Use Unsplash URL from `hero_image_url` field as background image with `loading="eager"`. Use the same page-hero CSS class already in servizi.css/servizio.css (duplicate the styles into chi-sono.css, same as Phase 2 decision about page-hero duplication).

2. **Bio Section** (`<section class="section" id="biografia">`) — two-column layout `.cs-bio` with:
   - Left: portrait image from `hero_image_url` (or a second field `portrait_url`) with `loading="lazy"`, wrapped in `.cs-bio__image`
   - Right: `bio_body()->kirbytext()` in `.cs-bio__text`

3. **Credentials Block** (`<section class="section section--grey" id="credenziali">`) — `.cs-credentials` with two items per RESEARCH Pattern 6:
   - Item 1: Lucide `award` icon + "Iscritto all'Ordine degli Avvocati di Padova" + `$page->ordine_numero()->or('(n. iscrizione)')` (TRUST-03)
   - Item 2: Lucide `map-pin` icon + "Giurisdizione" + "Opero presso il Tribunale di Este e il Tribunale di Padova" (TRUST-04)

4. **Values Section** (`<section class="section" id="valori">`) — section header with subtitle "Valori" + title from `valori_title`. Grid of value cards from `$page->valori()->toStructure()` — each with Lucide icon, title, description. 3-column grid on desktop, 1-column mobile.

5. **CTA Section** — reuse the same CTA pattern from servizio.php (Unsplash bg, overlay, "Hai bisogno di assistenza?", link to /contatti).

End with `snippet('footer')` and `snippet('scripts')`.

**Content file (chi-sono.txt):**
Create directory `content/chi-sono/` (no numeric prefix — pages are unlisted by default, will be listed in Phase 4 or manually in Panel). Actually, use `content/1_chi-sono/` with a numeric prefix so the page is listed and appears in sitemap.

Wait — check existing content structure: `content/home/` and `content/servizi/` have no numeric prefix. Follow the same pattern: `content/chi-sono/` without prefix. Kirby page listing is controlled by the directory naming — but the existing pages also lack prefixes. Keep consistent: `content/chi-sono/chi-sono.txt`.

Seed with professional Italian content:
- Title: Chi Sono
- hero_title: Avvocato Sebastiano Zanin
- hero_subtitle: Civilista con studio a Este. Assistenza legale in diritto di famiglia, immobiliare, risarcimento danni e recupero crediti.
- hero_image_url: https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=1920&q=80
- bio_body: Professional biography (3-4 paragraphs) covering: education, admission to Bar, experience areas, approach to client relationships. Mention both Tribunale di Este and Tribunale di Padova. NO outcome promises, NO testimonials per Art. 35 Codice Deontologico Forense. ASCII-only (no accented characters in field values, per Phase 2 convention).
- ordine_numero: (da confermare)
- giurisdizione: Opero presso il Tribunale di Este e il Tribunale di Padova, assistendo clienti in tutto il Veneto nelle materie del diritto civile.
- valori: 4 items — Professionalita (scale icon), Trasparenza (eye icon), Ascolto (ear icon), Risultati Concreti (target icon)
- CTA fields: cta_title "Hai Bisogno di Assistenza Legale?", cta_description "Prenota una consulenza...", cta_button_text "Contattaci", cta_button_url "contatti"

**CSS (chi-sono.css):**
Use `@layer layout { ... }` pattern per project convention.

Styles needed:
- `.page-hero` — duplicate from servizio.css (50vh min-height, absolute bg, overlay, content z-index). Same pattern, same decision as Phase 2.
- `.cs-bio` — two-column grid (1fr 1fr on desktop, stack on mobile). Gap 3rem.
- `.cs-bio__image img` — width 100%, border-radius var(--radius-md), object-fit cover, max-height 500px
- `.cs-bio__text` — uses body font, line-height 1.8
- `.cs-credentials` — flex column, gap 1.5rem, padding 2rem, background var(--color-grey-50) or section--grey handles it
- `.cs-credentials__item` — flex row, gap 1rem, align-items center. Icon in a 48px circle with navy bg + white icon.
- `.cs-values__grid` — 3-column grid on desktop (repeat(3, 1fr)), 1-col mobile. Gap 2rem.
- `.cs-values__card` — padding 2rem, text-align center, icon 40px, title in heading font
- Breadcrumb styles (reuse from servizio.css if needed, or they may be in main.css)

Responsive: stack bio columns below 768px, 2-col values grid at 768px, 3-col at 1024px.
  </action>
  <verify>
    <automated>php -l site/templates/chi-sono.php && php -l site/blueprints/pages/chi-sono.yml 2>&amp;1 || true; test -f content/chi-sono/chi-sono.txt && echo "content exists" || echo "MISSING content"; test -f assets/css/pages/chi-sono.css && echo "css exists" || echo "MISSING css"; grep -c "Ordine degli Avvocati di Padova" site/templates/chi-sono.php; grep -c "Tribunale di Este" site/templates/chi-sono.php</automated>
  </verify>
  <acceptance_criteria>
    - `site/templates/chi-sono.php` contains the string `Ordine degli Avvocati di Padova`
    - `site/templates/chi-sono.php` contains the string `Tribunale di Este e il Tribunale di Padova`
    - `site/templates/chi-sono.php` contains `snippet('head')` and `snippet('footer')` and `snippet('scripts')`
    - `site/templates/chi-sono.php` contains `id="main-content"`
    - `site/templates/chi-sono.php` contains `bio_body` and `valori` and `ordine_numero`
    - `site/blueprints/pages/chi-sono.yml` contains fields: `hero_title`, `bio_body`, `ordine_numero`, `giurisdizione`, `valori`, `meta_title`
    - `content/chi-sono/chi-sono.txt` contains `Title: Chi Sono`
    - `content/chi-sono/chi-sono.txt` contains `Tribunale di Este`
    - `content/chi-sono/chi-sono.txt` contains `ordine_numero`
    - `assets/css/pages/chi-sono.css` contains `@layer layout`
    - `assets/css/pages/chi-sono.css` contains `.cs-bio` and `.cs-credentials` and `.cs-values`
  </acceptance_criteria>
  <done>Chi Sono page renders at /chi-sono with bio, Ordine degli Avvocati badge (TRUST-03), jurisdiction statement (TRUST-04), and values grid. All images have explicit loading attributes.</done>
</task>

<task type="auto">
  <name>Task 2: Come Lavoro — blueprint, template, content, CSS</name>
  <files>
    site/blueprints/pages/come-lavoro.yml,
    site/templates/come-lavoro.php,
    content/come-lavoro/come-lavoro.txt,
    assets/css/pages/come-lavoro.css
  </files>
  <read_first>
    site/templates/servizio.php,
    site/blueprints/pages/servizio.yml,
    site/snippets/head.php,
    assets/css/pages/servizio.css,
    content/chi-sono/chi-sono.txt
  </read_first>
  <action>
**Blueprint (come-lavoro.yml):**
Tabbed blueprint with tabs: hero, contenuto, seo.

Hero tab:
- `hero_title` (text, required) — default "Come Lavoro"
- `hero_subtitle` (textarea, small)
- `hero_image_url` (url)

Contenuto tab:
- `intro_body` (textarea, large) — introductory text about methodology
- `process_steps` (structure) — fields: `step` (text, 1/6, placeholder "01"), `title` (text, 2/6, required), `description` (textarea, small, 3/6) — same field pattern as servizio.yml process_steps but this is a vertical timeline layout
- `cta_title` (text)
- `cta_description` (textarea, small)
- `cta_button_text` (text)
- `cta_button_url` (text)

SEO tab: `meta_title` (text), `meta_description` (textarea, small).

**Template (come-lavoro.php):**
Follow the same outer structure: `snippet('head')`, skip-link, `snippet('header')`, `<main id="main-content">`.

Sections:
1. **Page Hero** — same page-hero pattern as chi-sono (dark overlay, breadcrumb Home/Come Lavoro, h1, subtitle, Unsplash bg).

2. **Intro Section** (`<section class="section" id="intro">`) — centered editorial column `.cl-editorial` (max-width 780px, margin auto). Subtitle "Il Mio Metodo" + `intro_body()->kirbytext()`.

3. **Process Timeline** (`<section class="section section--grey" id="processo">`) — vertical timeline `.cl-timeline`. Section header: subtitle "Il Percorso Legale" + title "Come Funziona".
   For each `process_steps()->toStructure()`:
   ```html
   <div class="cl-timeline__step">
     <div class="cl-timeline__marker">
       <span class="cl-timeline__number">{step}</span>
     </div>
     <div class="cl-timeline__content">
       <h3>{title}</h3>
       <p>{description}</p>
     </div>
   </div>
   ```
   A vertical line connects the markers (CSS `::before` pseudo-element on the timeline container).

4. **CTA Section** — same pattern as chi-sono/servizio CTA (Unsplash bg, overlay, title, description, button).

End with `snippet('footer')` and `snippet('scripts')`.

**Content file (come-lavoro.txt):**
Create `content/come-lavoro/come-lavoro.txt`.

Seed with:
- Title: Come Lavoro
- hero_title: Come Lavoro
- hero_subtitle: Un approccio chiaro e strutturato per ogni questione legale
- hero_image_url: https://images.unsplash.com/photo-1450101499163-c8848c66ca85?w=1920&q=80
- intro_body: 2-3 paragraphs explaining the lawyer's methodology — transparent communication, clear timeline, client involvement at every step. NO outcome promises. ASCII-only.
- process_steps: 4 items (per TRUST-02):
  1. step: "01", title: "Consulenza Iniziale", description: "Analizziamo insieme la vostra situazione..."
  2. step: "02", title: "Conferimento Mandato", description: "Definiamo la strategia legale..."
  3. step: "03", title: "Gestione della Causa", description: "Seguiamo ogni fase del procedimento..."
  4. step: "04", title: "Raggiungimento del Risultato", description: "Lavoriamo per ottenere la migliore soluzione..."
- CTA: cta_title "Pronto a Iniziare?", cta_description "Prenota una consulenza iniziale per discutere il tuo caso.", cta_button_text "Contattaci", cta_button_url "contatti"

**CSS (come-lavoro.css):**
`@layer layout { ... }` wrapper.

Styles:
- `.page-hero` — duplicate from chi-sono.css (same page-hero pattern, loads on separate page)
- `.cl-editorial` — max-width 780px, margin 0 auto, text-align center for subtitle, left for body
- `.cl-timeline` — position relative, padding-left 60px (space for marker column)
- `.cl-timeline::before` — vertical line: position absolute, left 24px, top 0, bottom 0, width 2px, background var(--color-navy-200) or similar light navy
- `.cl-timeline__step` — position relative, padding-bottom 3rem, display flex, gap 1.5rem
- `.cl-timeline__marker` — width 48px, height 48px, border-radius 50%, background var(--color-navy), color white, display flex, align-items center, justify-content center, font-weight 700, flex-shrink 0, position relative, z-index 1
- `.cl-timeline__number` — font-family var(--font-heading), font-size 1.125rem
- `.cl-timeline__content h3` — font-family var(--font-heading), font-size 1.5rem, margin-bottom 0.5rem
- `.cl-timeline__content p` — color var(--color-text-muted), line-height 1.7

Responsive: timeline works well at all sizes since it's a single-column layout. Reduce padding-left on mobile (40px).
  </action>
  <verify>
    <automated>php -l site/templates/come-lavoro.php && echo "PHP OK"; test -f content/come-lavoro/come-lavoro.txt && echo "content exists" || echo "MISSING"; test -f assets/css/pages/come-lavoro.css && echo "css exists" || echo "MISSING"; grep -c "process_steps" site/templates/come-lavoro.php; grep -c "cl-timeline" assets/css/pages/come-lavoro.css</automated>
  </verify>
  <acceptance_criteria>
    - `site/templates/come-lavoro.php` contains `snippet('head')` and `snippet('footer')` and `snippet('scripts')`
    - `site/templates/come-lavoro.php` contains `id="main-content"`
    - `site/templates/come-lavoro.php` contains `process_steps` and `toStructure`
    - `site/templates/come-lavoro.php` contains `cl-timeline__step`
    - `site/blueprints/pages/come-lavoro.yml` contains `process_steps` with type `structure`
    - `content/come-lavoro/come-lavoro.txt` contains `Title: Come Lavoro`
    - `content/come-lavoro/come-lavoro.txt` contains `Consulenza Iniziale`
    - `content/come-lavoro/come-lavoro.txt` contains `Conferimento Mandato`
    - `assets/css/pages/come-lavoro.css` contains `@layer layout`
    - `assets/css/pages/come-lavoro.css` contains `.cl-timeline` and `.cl-timeline__step` and `.cl-timeline__marker`
  </acceptance_criteria>
  <done>Come Lavoro page renders at /come-lavoro with 4-step vertical timeline (consulenza, mandato, causa, risultato) per TRUST-02. Content is professional Italian, deontologically compliant.</done>
</task>

</tasks>

<verification>
- `php -l site/templates/chi-sono.php` — no syntax errors
- `php -l site/templates/come-lavoro.php` — no syntax errors
- Both content directories exist with `.txt` files
- Both CSS files exist and contain `@layer layout`
- Chi Sono template contains Ordine and Tribunale strings (TRUST-03, TRUST-04)
- Come Lavoro template iterates `process_steps` structure (TRUST-02)
</verification>

<success_criteria>
- /chi-sono renders with bio, credentials badge (Ordine Avvocati Padova), and jurisdiction (Tribunale di Este e Padova)
- /come-lavoro renders with 4-step vertical timeline (consulenza, mandato, causa, risultato)
- Both pages follow the established template pattern (head, header, main, footer, scripts)
- CSS uses @layer layout convention
- Content is professional Italian, no outcome promises, no testimonials
</success_criteria>

<output>
After completion, create `.planning/phases/03-supporting-pages/03-01-SUMMARY.md`
</output>
