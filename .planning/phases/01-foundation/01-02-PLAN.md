---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/css/variables.css
  - assets/css/reset.css
  - assets/css/base.css
  - assets/css/main.css
  - assets/css/utilities.css
  - assets/css/layout/global.css
  - assets/css/components/buttons.css
  - assets/css/components/cards.css
  - assets/css/components/forms.css
  - assets/css/components/breadcrumb.css
autonomous: true
requirements:
  - FOUND-02
  - FOUND-04
must_haves:
  truths:
    - "Navy #1a2744 and gold #b8960c are applied as CSS custom properties on :root"
    - "Cormorant Garamond is the display font variable and Plus Jakarta Sans is the body font variable"
    - "All breakpoints (375px, 768px, 1280px, 1440px) are defined as --bp-* variables"
    - "CSS @layer order is declared: reset, base, components, layout, utilities"
    - "Container, grid, section, skip-link, and scroll-top utility classes exist in global.css"
  artifacts:
    - path: "assets/css/variables.css"
      provides: "All design tokens as CSS custom properties"
      contains: "--color-primary: #1a2744"
    - path: "assets/css/main.css"
      provides: "CSS layer declaration and @import orchestration"
      contains: "@layer reset, base, components, layout, utilities"
    - path: "assets/css/layout/global.css"
      provides: "Container, section, grid, skip-link, scroll-top"
      contains: ".container"
  key_links:
    - from: "assets/css/main.css"
      to: "assets/css/variables.css"
      via: "@import"
      pattern: "@import.*variables.css"
    - from: "assets/css/main.css"
      to: "assets/css/reset.css"
      via: "@import url() layer(reset)"
      pattern: "@import.*reset.css.*layer"
---

<objective>
Build the complete CSS architecture: design tokens adapted for Avvocato Zanin's navy/gold legal palette, Josh Comeau reset, base element defaults, and all shared component/layout CSS layers.

Purpose: Every visual element in subsequent plans depends on these tokens. Establishing the palette and typography system now prevents rework later.

Output: A complete, layered CSS system (variables.css → reset → base → components → layout → utilities) with navy #1a2744 / gold #b8960c replacing studioatheste's blue/red palette.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md

<interfaces>
<!-- Source files to READ and ADAPT (do not copy blindly — swap colors) -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\variables.css -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\main.css -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\reset.css -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\base.css -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\utilities.css -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\layout\global.css -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\components\buttons.css -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\components\cards.css -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\components\forms.css -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\components\breadcrumb.css -->

Color substitution map (per D-01 and D-02):
- --color-primary: #074d99 → --color-primary: #1a2744  (navy)
- --color-primary-light: #1a6bc4 → --color-primary-light: #243a6e  (lighter navy)
- --color-primary-dark: #053a73 → --color-primary-dark: #111c30  (darker navy)
- --color-accent: #c8102e → --color-accent: #b8960c  (gold)
- --color-accent-light: #e01535 → --color-accent-light: #d4ab10  (lighter gold)

All other tokens (grays, shadows, spacing, typography, transitions, borders, breakpoints, layout) are IDENTICAL to studioatheste — copy verbatim per D-02.

Font families remain identical: Cormorant Garamond + Plus Jakarta Sans (no Libre Baskerville needed for this project).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create design tokens (variables.css) and CSS layer orchestrator (main.css)</name>
  <files>assets/css/variables.css, assets/css/main.css</files>
  <read_first>
    - C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\variables.css (full token set to adapt)
    - C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\main.css (layer + import structure)
  </read_first>
  <action>
Create `assets/css/` directory structure first:
```
assets/css/
  variables.css
  reset.css
  base.css
  main.css
  utilities.css
  layout/
    global.css
    header.css   (empty placeholder — Plan 04 fills this)
    footer.css   (empty placeholder — Plan 04 fills this)
  components/
    buttons.css
    cards.css
    forms.css
    breadcrumb.css
  pages/          (empty directory — future phases fill these)
```

**assets/css/variables.css** — Copy studioatheste's variables.css verbatim but apply color substitutions:
- `--color-primary: #1a2744` (navy — per D-01)
- `--color-primary-light: #243a6e` (lighter navy)
- `--color-primary-dark: #111c30` (darker navy)
- `--color-accent: #b8960c` (gold — per D-01)
- `--color-accent-light: #d4ab10` (lighter gold)
- ALL other tokens (grays, spacing, shadows, transitions, typography, borders, breakpoints, layout dimensions) copy verbatim from studioatheste
- Font variables remain: `--font-display: 'Cormorant Garamond', Georgia, serif` and `--font-body: 'Plus Jakarta Sans', system-ui, sans-serif`

**assets/css/main.css** — Copy studioatheste's main.css structure exactly, but remove page-specific imports that don't apply to Phase 1:
```css
@layer reset, base, components, layout, utilities;

@import url('variables.css');
@import url('reset.css') layer(reset);
@import url('base.css') layer(base);

/* Components */
@import url('components/buttons.css') layer(components);
@import url('components/cards.css') layer(components);
@import url('components/forms.css') layer(components);
@import url('components/breadcrumb.css') layer(components);

/* Layout */
@import url('layout/global.css') layer(layout);
@import url('layout/header.css') layer(layout);
@import url('layout/footer.css') layer(layout);

/* Utilities */
@import url('utilities.css') layer(utilities);
```

Note: No page-specific CSS imports yet (those are added in Phase 2/3). No badge.css or video-player.css (not needed for this project).
  </action>
  <verify>
    <automated>grep -n "color-primary" /c/Users/boxwe/Documents/GitHub/avvocatozanin/assets/css/variables.css | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `assets/css/variables.css` contains `--color-primary: #1a2744` (exact value)
    - `assets/css/variables.css` contains `--color-accent: #b8960c` (exact value)
    - `assets/css/variables.css` contains `--font-display: 'Cormorant Garamond'` (exact string)
    - `assets/css/variables.css` contains `--font-body: 'Plus Jakarta Sans'` (exact string)
    - `assets/css/variables.css` contains `--bp-sm: 375px` (exact value)
    - `assets/css/main.css` starts with `@layer reset, base, components, layout, utilities`
    - `assets/css/main.css` contains `@import url('variables.css')` WITHOUT a layer() wrapper (tokens are unlayered)
    - `assets/css/main.css` contains `@import url('reset.css') layer(reset)`
  </acceptance_criteria>
  <done>Design tokens file has navy/gold palette; main.css declares layer order correctly</done>
</task>

<task type="auto">
  <name>Task 2: Create reset.css, base.css, utilities.css, layout/global.css, and component CSS files</name>
  <files>
    assets/css/reset.css,
    assets/css/base.css,
    assets/css/utilities.css,
    assets/css/layout/global.css,
    assets/css/components/buttons.css,
    assets/css/components/cards.css,
    assets/css/components/forms.css,
    assets/css/components/breadcrumb.css,
    assets/css/layout/header.css,
    assets/css/layout/footer.css
  </files>
  <read_first>
    - C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\reset.css
    - C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\base.css
    - C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\utilities.css
    - C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\layout\global.css
    - C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\components\buttons.css
    - C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\components\cards.css
    - C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\components\forms.css
    - C:\Users\boxwe\Documents\GitHub\studioatheste\assets\css\components\breadcrumb.css
  </read_first>
  <action>
**assets/css/reset.css** — Copy verbatim from studioatheste. This is the Josh Comeau reset wrapped in `@layer reset {}`. No modifications needed.

**assets/css/base.css** — Copy from studioatheste's base.css. Apply one change in the `a` element default color:
- Change `color: var(--color-primary)` and `color: var(--color-primary-dark)` on hover — these will automatically reflect the navy tokens since the variables are already swapped in variables.css. No text changes needed here — just copy verbatim.

**assets/css/utilities.css** — Copy verbatim from studioatheste.

**assets/css/layout/global.css** — Copy from studioatheste's layout/global.css verbatim. It contains `.container`, `.section`, `.section--dark` (uses `--color-primary` which resolves to navy), `.grid`, `.skip-link`, and `.scroll-top`. The `.scroll-top` uses `--color-primary` (navy) and `.skip-link` uses `--color-primary` — correct for our design.

**assets/css/components/buttons.css** — Copy from studioatheste. Button styles use `--color-primary` and `--color-accent`. With our tokens swapped, `btn--primary` will be navy and `btn--accent` will be gold — correct.

**assets/css/components/cards.css** — Copy from studioatheste verbatim.

**assets/css/components/forms.css** — Copy from studioatheste verbatim. Form focus rings use `--color-primary-light` which resolves to lighter navy — correct.

**assets/css/components/breadcrumb.css** — Copy from studioatheste verbatim.

**assets/css/layout/header.css** — Create as an EMPTY placeholder file with just the layer wrapper comment:
```css
/* Header layout — populated in Plan 04 */
@layer layout {}
```

**assets/css/layout/footer.css** — Create as an EMPTY placeholder file:
```css
/* Footer layout — populated in Plan 04 */
@layer layout {}
```

These placeholders are needed so main.css @imports do not 404.
  </action>
  <verify>
    <automated>grep -c "@layer" /c/Users/boxwe/Documents/GitHub/avvocatozanin/assets/css/reset.css && grep "skip-link" /c/Users/boxwe/Documents/GitHub/avvocatozanin/assets/css/layout/global.css</automated>
  </verify>
  <acceptance_criteria>
    - `assets/css/reset.css` contains `@layer reset {` (wrapped in layer)
    - `assets/css/base.css` contains `font-family: var(--font-body)` on body (grep-verifiable)
    - `assets/css/base.css` contains `font-family: var(--font-display)` for h1/h2 headings (grep-verifiable)
    - `assets/css/layout/global.css` contains `.skip-link` class (grep-verifiable)
    - `assets/css/layout/global.css` contains `.scroll-top` class (grep-verifiable)
    - `assets/css/layout/global.css` contains `.container` class (grep-verifiable)
    - `assets/css/components/buttons.css` contains `.btn` class (grep-verifiable)
    - `assets/css/layout/header.css` file exists (even if empty placeholder)
    - `assets/css/layout/footer.css` file exists (even if empty placeholder)
  </acceptance_criteria>
  <done>All CSS layer files created; header.css and footer.css exist as placeholders for Plan 04</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep "--color-primary: #1a2744" assets/css/variables.css` returns a match
2. `grep "--color-accent: #b8960c" assets/css/variables.css` returns a match
3. All files referenced in main.css @imports exist (no 404s when loaded)
4. `grep "@layer reset, base" assets/css/main.css` returns a match
</verification>

<success_criteria>
- Navy #1a2744 + gold #b8960c are the primary/accent tokens
- CSS layer order is correctly declared before any @imports
- All component and layout CSS files exist (even if some are placeholders)
- Fonts: Cormorant Garamond (display) + Plus Jakarta Sans (body) are the font variables
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
