---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 01-03
  - 01-04
files_modified:
  - site/templates/home.php
  - assets/css/pages/homepage.css
  - assets/img/logo-studio-legale-zanin.png
autonomous: false
requirements:
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - NAV-05
  - NAV-06
must_haves:
  truths:
    - "The site renders at http://localhost:8000 without PHP errors or white screen"
    - "The header is visible with the Zanin logo, navy transparent background, and navigation links"
    - "Scrolling down 50px makes the header transition to solid white background"
    - "The Servizi mega-dropdown opens on hover on desktop showing 4 practice areas"
    - "The mobile hamburger menu opens a full-screen navy overlay with staggered link animation"
    - "The footer renders with 3 columns and real contact data (Via G.B. Brunelli 12, Este)"
    - "Skip-to-content link is focusable with Tab key"
    - "Scroll-to-top button appears after scrolling down"
    - "No console errors on initial page load"
    - "On a mobile viewport (375px) the layout is single-column and readable"
  artifacts:
    - path: "site/templates/home.php"
      provides: "Homepage template using head/header/footer/scripts snippets"
      contains: "snippet('head')"
    - path: "assets/img/logo-studio-legale-zanin.png"
      provides: "Logo file present for rendering"
  key_links:
    - from: "site/templates/home.php"
      to: "site/snippets/head.php + header.php + footer.php + scripts.php"
      via: "snippet() calls"
      pattern: "snippet.*head"
---

<objective>
Wire everything together: create the homepage template, add the logo image asset, and do a human visual verification of the complete navigation shell before proceeding to Phase 2 content pages.

Purpose: Confirm all Phase 1 pieces work together in a real browser before building on top of them. Catch CSS, JS, or template errors now.

Output: A running local Kirby site where the header, footer, animations, and navigation shell all function correctly at all viewport sizes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@.planning/phases/01-foundation/01-04-SUMMARY.md

<interfaces>
<!-- Reference template structure -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\site\templates\default.php -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\site\templates\home.php (if exists) -->

Loading screen HTML to add to home template (per D-10):
```html
<div class="loading-screen" id="loading-screen" aria-hidden="true">
  <div class="loading-screen__inner">
    <img src="<?= url('assets/img/logo-studio-legale-zanin.png') ?>" alt="Studio Legale Zanin" class="loading-screen__logo" width="200">
  </div>
</div>
```

Loading screen CSS (add to homepage.css or a global section):
```css
.loading-screen {
  position: fixed;
  inset: 0;
  background: var(--color-primary);
  z-index: 9999;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: opacity 0.6s var(--ease-out), visibility 0.6s var(--ease-out);
}
.loading-screen--hidden {
  opacity: 0;
  visibility: hidden;
  pointer-events: none;
}
.loading-screen__logo {
  filter: brightness(0) invert(1);
  animation: loading-pulse 1.2s ease-in-out infinite;
}
@keyframes loading-pulse {
  0%, 100% { opacity: 0.6; transform: scale(1); }
  50% { opacity: 1; transform: scale(1.04); }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create homepage template, homepage CSS, and copy logo asset</name>
  <files>site/templates/home.php, assets/css/pages/homepage.css, assets/img/logo-studio-legale-zanin.png</files>
  <read_first>
    - C:\Users\boxwe\Documents\GitHub\studioatheste\site\templates\default.php (snippet call pattern)
    - C:\Users\boxwe\Documents\GitHub\avvocatozanin\site\snippets\head.php (verify pageCss map has 'home' key)
    - C:\Users\boxwe\Documents\GitHub\avvocatozanin\content\home\home.txt (fields available for template)
  </read_first>
  <action>
**Logo asset** — The logo already exists on the existing avvocatozanin.it website. Check if it's already in `assets/img/`:
```bash
ls /c/Users/boxwe/Documents/GitHub/avvocatozanin/assets/img/
```
If `logo-studio-legale-zanin.png` is NOT present, create the `assets/img/` directory. The CONTEXT.md states the logo is at `assets/img/logo-studio-legale-zanin.png` (500x141, transparent PNG). If it's missing, the executor must notify the user and use a temporary CSS placeholder until the file is provided. Do NOT fabricate the image.

**assets/css/pages/homepage.css** — Create with loading screen CSS:
```css
/* Homepage — Loading Screen + minimal hero placeholder */
@layer layout {

  /* Loading Screen (session-gated, first visit only — per D-10) */
  .loading-screen {
    position: fixed;
    inset: 0;
    background: var(--color-primary);
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: opacity var(--duration-slow) var(--ease-out),
                visibility var(--duration-slow) var(--ease-out);
  }

  .loading-screen--hidden {
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
  }

  .loading-screen__inner {
    text-align: center;
  }

  .loading-screen__logo {
    filter: brightness(0) invert(1);
    width: 200px;
    height: auto;
    animation: loading-pulse 1.2s ease-in-out infinite;
  }

  @keyframes loading-pulse {
    0%, 100% { opacity: 0.6; transform: scale(1); }
    50% { opacity: 1; transform: scale(1.04); }
  }

  /* Hero placeholder (Phase 2 will replace this with full hero section) */
  .home-hero-placeholder {
    min-height: 100svh;
    background: var(--color-primary);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-white);
    text-align: center;
    padding: var(--space-8) var(--space-5);
  }

}
```

**site/templates/home.php** — Homepage template with loading screen and placeholder content:
```php
<?php snippet('head') ?>

  <!-- Skip-to-content accessibility link -->
  <a href="#main-content" class="skip-link">Vai al contenuto principale</a>

  <!-- Loading Screen (first-visit only — controlled by main.js) -->
  <div class="loading-screen" id="loading-screen" aria-hidden="true">
    <div class="loading-screen__inner">
      <img
        src="<?= url('assets/img/logo-studio-legale-zanin.png') ?>"
        alt="Studio Legale Zanin"
        class="loading-screen__logo"
        width="200"
        height="56"
      >
    </div>
  </div>

  <?php snippet('header') ?>

  <main id="main-content">

    <!-- Hero placeholder — Phase 2 builds the full hero -->
    <section class="home-hero-placeholder">
      <div>
        <p style="font-family: var(--font-body); font-size: var(--fs-small); letter-spacing: 0.1em; text-transform: uppercase; opacity: 0.6; margin-bottom: var(--space-4);">
          <?= esc($page->hero_eyebrow()) ?>
        </p>
        <h1 style="font-family: var(--font-display); font-size: var(--fs-hero); color: var(--color-white); margin-bottom: var(--space-5);">
          <?= $page->hero_title() ?>
        </h1>
        <p style="font-size: var(--fs-body); opacity: 0.8; max-width: 40ch; margin-inline: auto;">
          <?= esc($page->hero_subtitle()) ?>
        </p>
      </div>
    </section>

  </main>

  <?php snippet('footer') ?>

<?php snippet('scripts') ?>
```

After creating files, start the dev server and confirm no PHP errors:
```bash
cd /c/Users/boxwe/Documents/GitHub/avvocatozanin && php -S localhost:8000 router.php 2>&1 &
sleep 2
curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/
```
Expected: 200

Then kill the background server:
```bash
pkill -f "php -S localhost:8000" 2>/dev/null || true
```
  </action>
  <verify>
    <automated>grep -n "snippet('head')" /c/Users/boxwe/Documents/GitHub/avvocatozanin/site/templates/home.php && grep -n "loading-screen" /c/Users/boxwe/Documents/GitHub/avvocatozanin/assets/css/pages/homepage.css</automated>
  </verify>
  <acceptance_criteria>
    - `site/templates/home.php` contains `snippet('head')` (grep-verifiable)
    - `site/templates/home.php` contains `snippet('header')` (grep-verifiable)
    - `site/templates/home.php` contains `snippet('footer')` (grep-verifiable)
    - `site/templates/home.php` contains `snippet('scripts')` (grep-verifiable)
    - `site/templates/home.php` contains `loading-screen` div (grep-verifiable)
    - `site/templates/home.php` contains `id="main-content"` on main element (grep-verifiable)
    - `assets/css/pages/homepage.css` contains `.loading-screen` class (grep-verifiable)
    - `assets/css/pages/homepage.css` contains `loading-pulse` keyframe animation (grep-verifiable)
    - Starting dev server (`php -S localhost:8000 router.php`) produces no PHP parse errors in stderr
  </acceptance_criteria>
  <done>Homepage template wires all snippets; loading screen CSS defined; dev server starts without errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 1 foundation: Kirby 5 installed, navy/gold design tokens, GSAP+Lenis initialized, header (topbar, sticky, mega-dropdown, mobile overlay), footer (3-column, contact data), skip-to-content, scroll-to-top, and loading screen.
  </what-built>
  <how-to-verify>
1. Start the dev server: `cd /c/Users/boxwe/Documents/GitHub/avvocatozanin && php -S localhost:8000 router.php`
2. Open http://localhost:8000 in a browser

**Desktop checks (1280px+):**
- Loading screen shows navy background with logo on FIRST visit; does NOT show on refresh
- Header starts transparent over the hero placeholder (navy section blends in)
- Scroll down 50px → header transitions to solid white background
- Hover over "Servizi" in nav → mega-dropdown opens with 4 practice areas (Famiglia, Immobiliare, Danni, Crediti) with Lucide icons and stagger animation
- Hover off → dropdown closes
- Phone number in topbar is a clickable `tel:` link
- Footer shows 3 columns: logo+social, navigation links, contact info with gold Lucide icons
- "Via G.B. Brunelli 12, 35042 Este (PD)" is visible in footer
- Scroll to bottom → scroll-to-top button appears (bottom-right)
- Click scroll-to-top → smooth scroll to top via Lenis
- Open browser console → zero JavaScript errors

**Mobile checks (375px viewport):**
- Hamburger button visible (3 bars); desktop nav hidden
- Click hamburger → full-screen navy overlay slides in with staggered link animation
- Click X → menu closes
- Tap any menu link → menu closes
- Footer stacks to single column
- Tap phone number in menu or footer → tel: link activates

**Accessibility checks:**
- Tab key → skip-to-content link appears at top
- Press Enter on skip link → focus jumps to #main-content
- Hover Servizi dropdown, press Escape → dropdown closes and focus returns to trigger
- Press Escape when mobile menu open → menu closes, focus returns to hamburger
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found (CSS rendering, JS errors, layout problems)</resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when the human checkpoint is approved. All automated verifications from Plans 01-04 have already run.
</verification>

<success_criteria>
- Kirby 5 runs locally without errors (Plan 01)
- Navy/gold design tokens and CSS layer architecture in place (Plan 02)
- GSAP+Lenis initialized, prefers-reduced-motion respected, Lucide icons load (Plan 03)
- Header: topbar, transparent→solid, mega-dropdown (4 Servizi), mobile overlay (Plan 04)
- Footer: 3 columns, real contact data, PEC, Ordine degli Avvocati reference (Plan 04)
- Skip-to-content and scroll-to-top functional (Plans 03/04)
- Loading screen on first visit only (Plan 05)
- Human visual verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
