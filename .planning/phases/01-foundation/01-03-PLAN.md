---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - site/snippets/head.php
  - site/snippets/scripts.php
  - assets/js/scroll.js
  - assets/js/animations.js
  - assets/js/main.js
  - assets/js/components/scroll-top.js
autonomous: true
requirements:
  - FOUND-03
  - FOUND-05
must_haves:
  truths:
    - "head.php outputs a complete HTML head with meta tags, Google Fonts (Cormorant Garamond + Plus Jakarta Sans), Lenis CSS, and main.css"
    - "scripts.php loads CDN scripts in the correct order: GSAP → ScrollTrigger → Lenis → Lucide → local JS"
    - "scroll.js initializes Lenis with lerp:0.1, registers ScrollTrigger, syncs scroll events, and exports window.lenis"
    - "When prefers-reduced-motion is active, GSAP animations are skipped (early return in animations.js)"
    - "main.js calls lucide.createIcons() on DOMContentLoaded"
  artifacts:
    - path: "site/snippets/head.php"
      provides: "HTML head: charset, viewport, meta title fallback chain, OG tags, font preconnects, Lenis CSS, main.css"
      contains: "fonts.googleapis.com"
    - path: "site/snippets/scripts.php"
      provides: "CDN + local JS loading in correct order"
      contains: "gsap.min.js"
    - path: "assets/js/scroll.js"
      provides: "Lenis + ScrollTrigger integration"
      contains: "window.lenis = lenis"
    - path: "assets/js/animations.js"
      provides: "GSAP animation module with prefers-reduced-motion guard"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "site/snippets/scripts.php"
      to: "assets/js/scroll.js"
      via: "js() Kirby helper"
      pattern: "js.*scroll.js"
    - from: "assets/js/scroll.js"
      to: "window.lenis"
      via: "global assignment"
      pattern: "window.lenis = lenis"
    - from: "assets/js/navigation.js"
      to: "window.lenis"
      via: "consumption in Plan 04"
      pattern: "window.lenis"
---

<objective>
Create the HTML head snippet (meta tags, fonts, CSS loading), the scripts footer snippet (CDN + local JS loading), and all JavaScript modules (scroll infrastructure, animations scaffold, main entry, scroll-top).

Purpose: These snippets are included in every template. The JS modules establish the animation system that all Phase 2/3 page animations depend on.

Output: Working head.php and scripts.php snippets, plus all JS files that handle Lenis smooth scroll, GSAP initialization, prefers-reduced-motion, and scroll-to-top.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Reference files to adapt (READ-ONLY) -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\site\snippets\head.php -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\site\snippets\scripts.php -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\assets\js\scroll.js -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\assets\js\animations.js -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\assets\js\main.js -->
<!-- C:\Users\boxwe\Documents\GitHub\studioatheste\assets\js\components\scroll-top.js -->

CDN load order (per D-14 — EXACT ORDER IS CRITICAL):
1. https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js
2. https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/ScrollTrigger.min.js
3. https://unpkg.com/lenis@1.3.17/dist/lenis.min.js
4. https://unpkg.com/lucide@latest
5. assets/js/scroll.js
6. assets/js/animations.js?v=filemtime
7. assets/js/components/scroll-top.js
8. assets/js/navigation.js
9. assets/js/main.js
10. (page-specific JS — conditional by template)

Google Fonts URL for this project (Cormorant Garamond + Plus Jakarta Sans — no Libre Baskerville):
https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;0,700;1,400;1,600&family=Plus+Jakarta+Sans:ital,wght@0,200..800;1,200..800&display=swap

Lenis CSS CDN: https://unpkg.com/lenis@1.3.17/dist/lenis.css

Page-specific CSS map for head.php (populate only templates that will exist after Phase 2/3 — add as comment placeholders now):
- 'home' => 'assets/css/pages/homepage.css'
- 'servizi' => 'assets/css/pages/servizi.css'
- 'servizio' => 'assets/css/pages/servizio.css'
- 'contatti' => 'assets/css/pages/contatti.css'
- 'chi-sono' => 'assets/css/pages/chi-sono.css'
- 'come-lavoro' => 'assets/css/pages/come-lavoro.css'

Page-specific JS map for scripts.php:
- 'contatti' => ['assets/js/pages/contatti.js']
- 'home' => ['assets/js/pages/home.js']
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create head.php and scripts.php snippets</name>
  <files>site/snippets/head.php, site/snippets/scripts.php</files>
  <read_first>
    - C:\Users\boxwe\Documents\GitHub\studioatheste\site\snippets\head.php (meta, fonts, CSS loading pattern)
    - C:\Users\boxwe\Documents\GitHub\studioatheste\site\snippets\scripts.php (CDN + local JS loading)
    - C:\Users\boxwe\Documents\GitHub\avvocatozanin\content\site.txt (site title to verify)
  </read_first>
  <action>
**site/snippets/head.php** — Adapt from studioatheste head.php. Changes from the reference:
1. Remove Libre Baskerville from Google Fonts URL — only Cormorant Garamond + Plus Jakarta Sans
2. Update page-specific CSS map to use this project's template names (home, servizi, servizio, contatti, chi-sono, come-lavoro)
3. Keep all meta tag logic identical: fallback chain for title ($page->meta_title → $page->title), description, OG tags, favicon link
4. Lenis CSS CDN and main.css links are identical

The `$pageCss` array should be:
```php
$pageCss = [
  'home'        => 'assets/css/pages/homepage.css',
  'servizi'     => 'assets/css/pages/servizi.css',
  'servizio'    => 'assets/css/pages/servizio.css',
  'contatti'    => 'assets/css/pages/contatti.css',
  'chi-sono'    => 'assets/css/pages/chi-sono.css',
  'come-lavoro' => 'assets/css/pages/come-lavoro.css',
];
```

The conditional `if (isset($pageCss[$template]))` prevents loading CSS files that don't exist yet — safe for current phase.

**site/snippets/scripts.php** — Adapt from studioatheste scripts.php:
1. CDN order stays identical (per D-14)
2. Update page-specific JS map:
```php
$pageJs = [
  'contatti' => ['assets/js/pages/contatti.js'],
  'home'     => ['assets/js/pages/home.js'],
];
```
3. Close the `</body></html>` at end of file as in the reference

The `animations.js` line MUST include filemtime cache-busting:
```php
<?= js('assets/js/animations.js?v=' . filemtime(kirby()->root() . '/assets/js/animations.js')) ?>
```
  </action>
  <verify>
    <automated>grep -n "fonts.googleapis.com" /c/Users/boxwe/Documents/GitHub/avvocatozanin/site/snippets/head.php && grep -n "gsap.min.js" /c/Users/boxwe/Documents/GitHub/avvocatozanin/site/snippets/scripts.php</automated>
  </verify>
  <acceptance_criteria>
    - `site/snippets/head.php` contains `fonts.googleapis.com` (Google Fonts preconnect)
    - `site/snippets/head.php` contains `Cormorant+Garamond` in the fonts URL
    - `site/snippets/head.php` contains `Plus+Jakarta+Sans` in the fonts URL
    - `site/snippets/head.php` does NOT contain `Libre+Baskerville` (removed from reference)
    - `site/snippets/head.php` contains `lenis.css` CDN link
    - `site/snippets/head.php` contains `css('assets/css/main.css')` Kirby helper call
    - `site/snippets/scripts.php` contains `gsap/3.12.5/gsap.min.js` (exact CDN URL)
    - `site/snippets/scripts.php` contains `gsap/3.12.5/ScrollTrigger.min.js` (exact CDN URL)
    - `site/snippets/scripts.php` contains `lenis@1.3.17/dist/lenis.min.js` (exact CDN URL)
    - `site/snippets/scripts.php` contains `lucide@latest` (CDN)
    - `site/snippets/scripts.php` contains `animations.js?v=` (cache-busting present)
  </acceptance_criteria>
  <done>head.php and scripts.php created; font URL correct; CDN load order matches D-14</done>
</task>

<task type="auto">
  <name>Task 2: Create JS modules — scroll.js, animations.js, main.js, scroll-top.js</name>
  <files>
    assets/js/scroll.js,
    assets/js/animations.js,
    assets/js/main.js,
    assets/js/components/scroll-top.js
  </files>
  <read_first>
    - C:\Users\boxwe\Documents\GitHub\studioatheste\assets\js\scroll.js (Lenis + ScrollTrigger integration — copy verbatim)
    - C:\Users\boxwe\Documents\GitHub\studioatheste\assets\js\animations.js (prefers-reduced-motion pattern + animation stubs)
    - C:\Users\boxwe\Documents\GitHub\studioatheste\assets\js\main.js (lucide.createIcons init)
    - C:\Users\boxwe\Documents\GitHub\studioatheste\assets\js\components\scroll-top.js (scroll-to-top via Lenis)
  </read_first>
  <action>
Create `assets/js/` directory and `assets/js/components/` subdirectory.

**assets/js/scroll.js** — Copy VERBATIM from studioatheste. The initialization order is critical (per D-14 comment in scroll.js):
1. `gsap.registerPlugin(ScrollTrigger)`
2. `new Lenis({ lerp: 0.1, smoothWheel: true, anchors: true })`
3. `lenis.on('scroll', ScrollTrigger.update)`
4. `gsap.ticker.add(function(time) { lenis.raf(time * 1000); })`
5. `gsap.ticker.lagSmoothing(0)`
6. `window.lenis = lenis`

Do NOT modify scroll.js — copy it byte-for-byte.

**assets/js/animations.js** — Copy the STRUCTURE from studioatheste but keep only the Phase 1-relevant parts:
- The prefers-reduced-motion guard (MUST keep): `if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;`
- The splitWords helper function
- Remove all page-specific animations (homepage parallax, stats counter, etc.) — those go in Phase 2/3 page JS files
- Keep the IIFE wrapper `(function() { 'use strict'; ... })()`
- Add a comment: `/* Page-specific animations added in Phase 2+ */`

The file should be approximately:
```javascript
(function() {
  'use strict';

  // Respect user's reduced motion preference (per D-15)
  if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;

  /* Helper: split text into word spans for stagger animation */
  function splitWords(el) {
    var parts = el.innerHTML.split(/<br\s*\/?>/gi);
    var result = parts.map(function(part) {
      var text = part.replace(/&nbsp;/g, ' ').replace(/<[^>]+>/g, '').trim();
      if (!text) return '';
      return text.split(/\s+/).map(function(w) {
        return '<span class="split-word-wrap"><span class="split-word">' + w + '</span></span>';
      }).join(' ');
    }).join('<br>');
    el.innerHTML = result;
    return el.querySelectorAll('.split-word');
  }

  // Export for use in page-specific JS modules
  window.splitWords = splitWords;

  /* Page-specific animations added in Phase 2+ */

})();
```

**assets/js/main.js** — Adapt from studioatheste: initialize Lucide icons, add loading screen logic (per D-10). The loading screen uses sessionStorage to show only on first visit:
```javascript
document.addEventListener('DOMContentLoaded', function() {
  // Initialize Lucide icons (per D-16)
  lucide.createIcons();

  // Loading screen — show only on first visit this session (per D-10)
  var loadingScreen = document.querySelector('.loading-screen');
  if (loadingScreen) {
    if (!sessionStorage.getItem('visited')) {
      sessionStorage.setItem('visited', '1');
      // Loading screen visible via CSS; hide after delay
      setTimeout(function() {
        loadingScreen.classList.add('loading-screen--hidden');
      }, 1500);
    } else {
      loadingScreen.style.display = 'none';
    }
  }

  console.log('Studio Legale Zanin — inizializzato');
});
```

**assets/js/components/scroll-top.js** — Copy VERBATIM from studioatheste. Uses `window.lenis.scrollTo(0, { duration: 1.2 })` and ScrollTrigger for show/hide at 500px scroll threshold.
  </action>
  <verify>
    <automated>grep -n "window.lenis = lenis" /c/Users/boxwe/Documents/GitHub/avvocatozanin/assets/js/scroll.js && grep -n "prefers-reduced-motion" /c/Users/boxwe/Documents/GitHub/avvocatozanin/assets/js/animations.js</automated>
  </verify>
  <acceptance_criteria>
    - `assets/js/scroll.js` contains `window.lenis = lenis` (global export — grep-verifiable)
    - `assets/js/scroll.js` contains `gsap.registerPlugin(ScrollTrigger)` as first non-comment line (grep-verifiable)
    - `assets/js/scroll.js` contains `lerp: 0.1` (Lenis config — grep-verifiable)
    - `assets/js/scroll.js` contains `gsap.ticker.lagSmoothing(0)` (grep-verifiable)
    - `assets/js/animations.js` contains `prefers-reduced-motion: reduce` (guard — grep-verifiable)
    - `assets/js/animations.js` contains `window.splitWords = splitWords` (exported helper — grep-verifiable)
    - `assets/js/main.js` contains `lucide.createIcons()` (grep-verifiable)
    - `assets/js/main.js` contains `sessionStorage` (loading screen logic — grep-verifiable)
    - `assets/js/components/scroll-top.js` contains `window.lenis.scrollTo(0` (grep-verifiable)
    - `assets/js/components/scroll-top.js` file exists at the path listed in scripts.php
  </acceptance_criteria>
  <done>All JS modules created; scroll.js exports window.lenis; animations.js has reduced-motion guard; main.js initializes Lucide</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep "Cormorant+Garamond" site/snippets/head.php` — returns match
2. `grep "window.lenis = lenis" assets/js/scroll.js` — returns match
3. `grep "prefers-reduced-motion" assets/js/animations.js` — returns match
4. `grep "lucide.createIcons" assets/js/main.js` — returns match
5. `grep "lenis.min.js" site/snippets/scripts.php` — returns match
</verification>

<success_criteria>
- head.php loads correct fonts (no Libre Baskerville) and main.css
- scripts.php loads CDNs in correct order (GSAP → ScrollTrigger → Lenis → Lucide → local JS)
- scroll.js initializes Lenis with lerp:0.1 and exports window.lenis
- animations.js has prefers-reduced-motion guard as first code after IIFE wrapper
- main.js calls lucide.createIcons() and handles loading screen via sessionStorage
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
